import { describe, it, expect } from 'vitest'
import LoginPage from '../../../auth/LoginPage'

describe('LoginPage', () => {
  it('default export is undefined since file has no content', () => {
    expect(LoginPage).toBeUndefined()
  })
})
